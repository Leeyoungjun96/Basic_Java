~.java → ~.class (코드를 메모리에 저장하는 것을 loading이라고 함)

메소드(클래스에 작성한 코드가 저장)

힙(참조변수가 가르키는 곳, 객체가 생성되는 곳, Field만 들어감(메소드는 못들어감))

JVM스택(변수 선언시 이곳에 저장)

int var1 = 2; (스레드에 바로 저장)

String var2 = "a"; (힙에서 저장, 스레드에서 var2로 "a"의 힙에서의 주소를 저장)

Stack      | heap

---

var1        |  "a"(100)

var2(100)|  

int x = null;   (x)
String x = null;(O) 참조하는 객체가 없다(주소가 없다)

기본타입 변수에는 null값을 넣을 수 없지만 참조타입 변수에는 null값을 넣을 수 있다.
